# LLM Wrapper — Phase 1

Quickstart (`research-harness/`):

```bash
python -m venv .venv && source .venv/bin/activate
pip install -e ".[dev]"
pytest
```

No secrets in repo — set `LOCAL_LLM_BASE_URL` / `API_KEY` via env.
